import img1 from '../../images/img1.jpg'
import img2 from '../../images/img2.jpg'
<!-- the follow code demonstrate you can use inline css, by the way less and scss also be supported -->
style.scss {
  .chart {
    position: relative;
    display: inline-block;
    width: 110px;
    height: 110px;
    margin-top: 50px;
    margin-bottom: 50px;
    text-align: center;
  }
  .chart canvas {
    position: absolute;
    top: 0;
    left: 0;
  }
  .percent {
    display: inline-block;
    line-height: 110px;
    z-index: 2;
  }
  .percent:after {
    content: '%';
    margin-left: 0.1em;
    font-size: .8em;
  }
}
<div class="container">
  <div class="row">
    <div class="col-md-12">
      <h1>
        Marko4 + Webpack3 + Babel7 <small>Support ES6</small>
      </h1>
    </div>
    <div class="col-md-12">
      <h2>1. Test import image in marko file</h2>
      <img src="${img2}" class="img-responsive"/>
    </div>
    <div class="col-md-12">
      <h2>2. Test background</h2>
      <div class="test-background"/>
    </div>
    <div class="col-md-12">
      <h2>3. Test vdom</h2>
      <div>
        Counter in current template: <b>${input.initCount}</b>
      </div>
      <app-counter(input.initCount)/>
      <if(!input.hideBtn)>
        <div class="btn-group" role="group" aria-label="...">
          <button on-click('inc') type="button" class="btn btn-default">+</button>
          <button on-click('dec') type="button" class="btn btn-default">-</button>
        </div>
      </if>
    </div>
    <div class="col-md-12">
      <h2>
        4. Two way data-binding <small>Try to type any character in the following textbox</small>
      </h2>
      <div class="row">
        <div class="form-group">
          <div class="col-md-4">
            <input class="form-control" value=state.content type="text"
              on-input("handleInput")/>
          </div>
          <div class="col-md-8">
            <p class="form-control-static">
              <b>${state.content}</b>
            </p>
          </div>
        </div>
      </div>
    </div>
    <div class="col-md-12">
      <h2>5. Test integrate jquery plugin</h2>
      <span class="chart" no-update>
        <span class="percent">${Math.round(state.percent)}</span>
      </span>
      <span class="btn btn-default" on-click('updateChart')>Update chart</span>
    </div>
    <div class="col-md-12">
      <h2>6. Test icon font</h2>
      <span class="glyphicon glyphicon-heart" style="font-size:80px"/>
      <p>glyphicon glyphicon-heart</p>
    </div>
    <div class="col-md-12">
      <h2>7. Test plain javascript library</h2>
      <a class="btn btn-default" href="javascript:;" role="button"
        on-click('showToastr')>Click me to show toastr</a>
    </div>
    <div class="col-md-12">
      <h2>
        8. Test mixin <small>The history-mixin attach a history instance to component</small>
      </h2>
      <p>
        Before click the following 2 links, open dev-tools firstly, then take notice on console output and address bar
      </p>
      <a on-click('gotoTest') href="/">Home Page Link</a>&nbsp;&nbsp;&nbsp;&nbsp;
      <a on-click('gotoMixin') href="/test">Test Page Link</a>
    </div>
  </div>
</div>
